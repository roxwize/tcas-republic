<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/static/css/style.css">
    <link rel="stylesheet" href="/static/css/silk.css">
    {% if title %}
      <title>{{ title | safe }} | {{ global.title | safe }}</title>
    {% else %}
      <title>{{ global.title | safe }}</title>
    {% endif %}
  </head>
  <body>
    <main>
      <header>
        <h1>
          <a style="color:inherit;text-decoration:none;" href="/">
            The <span style="color:#08f;">Two</span><span style="color:#808;">Cans</span> Republic
          </a>
        </h1>
      </header>
      <div id="content">
        {{ content | safe }}
      </div>
    </main>
  </body>
</html>
